services:
  roscro-web:
    build: .
    container_name: roscro-industries
    restart: unless-stopped
    ports:
      - "3030:3030"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
